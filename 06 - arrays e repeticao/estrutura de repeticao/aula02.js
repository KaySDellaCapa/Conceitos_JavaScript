// Executando For em uma String

const nome = 'Kayque S. Della Capa'

for (let i = 0; i < nome.length; i++) { // for percorre
    const letra = nome[i];  // Nome na posição 'i'. É um array de caractere
    console.log(letra);
}